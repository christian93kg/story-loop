---
active_path:
---

# ▶ Active game

> Empty until `new-story` scaffolds your first one. This file is the single pointer
> the `craft-gate` hook follows every prompt (`active_path:` in the frontmatter above)
> — nothing else needs to change when you start or switch stories.

No story yet. Tell Claude *"let's start an adventure"* to invoke `new-story`.

---

## Shelved / past stories

> `finish-story` and `new-story` maintain this list. Each entry: slug, title, one-line
> status, and how to reverse it if shelved. Nothing here is ever deleted — a finished
> or shelved story moves to `_archive/`, and the note describing how to revive it stays.
